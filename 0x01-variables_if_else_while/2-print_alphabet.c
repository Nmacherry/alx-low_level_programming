#include <stdio.h>
/**
 * main - main block
 * Description: prints the alphabet
 * Return: 0
 */
int main(void)
{
	char alpha;

	for
		(alpha = 'a'; alpha <= 'z')
	{
		putchar(alpha);
		alpha++;
	}
	putchar('\n');
	return (0);
}
